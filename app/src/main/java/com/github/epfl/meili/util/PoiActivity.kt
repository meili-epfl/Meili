package com.github.epfl.meili.util

import android.content.Intent

open class PoiActivity(layout: Int, activityId: Int): NavigableActivity(layout, activityId) {

    override fun getNavigationIntent(id: Int): Intent {
        TODO()
    }
}